// import dependencies
import { Outlet } from "react-router";

const LandingMainLayout = () => {
    return (
        <div>
            <Outlet />
        </div>
    );
};

export default LandingMainLayout;
