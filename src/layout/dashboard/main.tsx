// import dependencies
import { Outlet } from "react-router";

const DashboardMainLayout = () => {
    return (
        <div>
            <Outlet />
        </div>
    );
};

export default DashboardMainLayout;
