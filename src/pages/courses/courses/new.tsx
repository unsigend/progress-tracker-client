import { CourseNewContainer } from "@/features/courses/container/courses/CourseNewContainer";

/**
 * CourseNewPage - The page for creating a new course
 * @returns CourseNewPage component
 */
export const CourseNewPage = () => {
    return <CourseNewContainer />;
};
