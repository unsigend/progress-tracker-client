import { CourseListContainer } from "@/features/courses/container/courses/CourseListContainer";

/**
 * CourseListPage - The page for the course list
 * @returns CourseListPage component
 */
export const CourseListPage = () => {
    return <CourseListContainer />;
};
