import { BookEditContainer } from "@/features/reading/container/books/BookEditContainer";

/**
 * BookEditPage - The page for editing a book
 * @returns BookEditPage component
 */
export const BookEditPage = () => {
    return <BookEditContainer />;
};
