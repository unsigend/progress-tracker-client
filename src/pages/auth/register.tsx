import { RegisterContainer } from "@/features/auth/container/RegisterContainer";

/**
 * AuthRegisterPage - The page for the register page
 * @returns AuthRegisterPage component
 */
export const AuthRegisterPage = () => {
    return <RegisterContainer />;
};
